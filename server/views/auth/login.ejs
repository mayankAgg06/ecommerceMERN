<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-slate-100 text-slate-900">
  <div class="max-w-md mx-auto px-4 py-10">
    <%- include("../partials/flash") %>

    <div class="bg-white border border-slate-200 rounded-2xl shadow-sm p-6">
      <h1 class="text-2xl font-semibold mb-6">Login</h1>

      <form action="/login" method="POST" class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-1" for="username">Username</label>
          <input class="w-full rounded-xl border border-slate-300 px-3 py-2"
                 type="text" name="username" id="username" required />
        </div>

        <div>
          <label class="block text-sm font-medium mb-1" for="password">Password</label>
          <input class="w-full rounded-xl border border-slate-300 px-3 py-2"
                 type="password" name="password" id="password" required />
        </div>

        <button class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-medium py-2.5 rounded-xl">
          Login
        </button>

        <p class="text-sm text-slate-600 mt-3">
          Don't have an account?
          <a class="text-indigo-600 hover:underline" href="/register">Sign Up</a>
        </p>
      </form>
    </div>
  </div>
</body>
</html>
