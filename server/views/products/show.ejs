<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= product.name %> | My Store</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<style type="text/tailwindcss">
    @theme {
    --color-clifford: #da373d;
    }
</style>
</head>
<body class="bg-slate-100 text-slate-900">
  <div class="min-h-screen">
    <!-- Page container -->
    <div class="max-w-5xl mx-auto px-4 py-8">
      <!-- Breadcrumb -->
      <nav class="text-sm mb-4" aria-label="Breadcrumb">
        <ol class="flex items-center gap-1 text-slate-500">
          <li>
            <a href="/" class="hover:text-slate-700">Home</a>
          </li>
          <li class="mx-1">/</li>
          <li>
            <a href="/products" class="hover:text-slate-700">Products</a>
          </li>
          <li class="mx-1">/</li>
          <li class="text-slate-700 font-medium truncate max-w-[150px]">
            <%= product.name %>
          </li>
        </ol>
      </nav>

      <!-- Card -->
      <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6 md:p-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- Left: Image -->
          <div class="flex items-center justify-center">
            <img
              src="<%= product.image %>"
              alt="<%= product.name %>"
              class="rounded-xl shadow-md max-h-96 object-cover w-full"
            />
          </div>

          <!-- Right: Details -->
          <div>
            <h1 class="text-2xl md:text-3xl font-semibold mb-2">
              <%= product.name %>
            </h1>

            <p class="text-xs text-slate-500 mb-1">
              Added on:
              <span class="font-medium text-slate-700">
                <%= new Date(product.dateCreated).toLocaleDateString('en-IN') %>
              </span>
            </p>

            <p class="text-sm text-slate-600 mb-3">
              Warranty:
              <span class="font-medium text-slate-800">
                <%= product.warranty %>
              </span>
            </p>

            <div class="flex items-center gap-3 mb-4">
              <p class="text-2xl font-bold text-indigo-600">
                ₹<%= product.price %>
              </p>

              <% if (product.isAvailable) { %>
                <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700">
                  In stock
                </span>
              <% } else { %>
                <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-rose-100 text-rose-700">
                  Out of stock
                </span>
              <% } %>
            </div>

            <p class="text-sm leading-relaxed text-slate-700 mb-6">
              <%= product.desc %>
            </p>

            <!-- Action buttons -->
            <div class="flex flex-wrap gap-3">
              <button
                type="button"
                class="inline-flex items-center justify-center px-5 py-2.5 rounded-full text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 transition disabled:opacity-50 disabled:cursor-not-allowed"
                <% if (!product.isAvailable) { %> disabled <% } %>
              >
                Add to Cart
              </button>

              <button
                type="button"
                class="inline-flex items-center justify-center px-5 py-2.5 rounded-full text-sm font-medium border border-slate-300 text-slate-800 bg-white hover:bg-slate-50 transition"
              >
                Buy Now
              </button>
              <a href="/products/<%=product._id%>/edit" class="btn btn-info inline-flex items-center justify-center px-5 py-2.5 
                rounded-full text-sm font-medium border border-slate-300 text-slate-800 bg-white hover:bg-slate-50 transition">Edit Product</a>
            </div>
          </div>
        </div>

        <!-- Divider -->
        <div class="h-px bg-slate-200 my-8"></div>

        <!-- Extra Product Details -->
        <div>
          <h2 class="text-lg font-semibold mb-4">
            Product details
          </h2>

          <div class="overflow-hidden rounded-xl border border-slate-200 bg-slate-50">
            <table class="w-full text-sm">
              <tbody>
                <tr class="border-b border-slate-200">
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Name
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    <%= product.name %>
                  </td>
                </tr>
                <tr class="border-b border-slate-200">
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Date created
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    <%= new Date(product.dateCreated).toLocaleString('en-IN') %>
                  </td>
                </tr>
                <tr class="border-b border-slate-200">
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Warranty
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    <%= product.warranty %>
                  </td>
                </tr>
                <tr class="border-b border-slate-200">
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Price
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    ₹<%= product.price %>
                  </td>
                </tr>
                <tr class="border-b border-slate-200">
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Availability
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    <% if (product.isAvailable) { %>
                      In stock
                    <% } else { %>
                      Out of stock
                    <% } %>
                  </td>
                </tr>
                <tr>
                  <th class="w-40 text-left px-4 py-3 font-medium text-slate-600 bg-slate-100">
                    Description
                  </th>
                  <td class="px-4 py-3 text-slate-800">
                    <%= product.desc %>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
